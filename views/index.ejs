<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../styles.css" />
    <title>Document</title>
  </head>
  <body>
    <nav>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
      <% if(currentUser){%>
      <a href="/dashboard">Dashboard</a>
      <a href="/log-out">Logout</a>
      <%}%>
    </nav>
    <h1>Home page</h1>
    <% if(currentUser) { %>
    <p>Logged in as user : <%= currentUser.username %></p>
    <% if(currentUser.id === "63a48c94b073ed35550a3f05") { %>
    <p>User type : Admin</p>
    <%} else { %>
    <p>User type : regular</p>
    <% } %> <%} %>
    <div class="post-container">
      <h3>Posts</h3>
      <% if(posts.length >0){ %> <%posts.forEach(post =>{ %>
      <div class="post-card">
        <% if(currentUser){ %>
        <h3>Author: <%=post.author %></h3>
        <% } else { %>
        <h2>Author: Anonymous</h2>
        <%} %>
        <h4><%=post.title%></h4>
        <p><%=post.post%></p>
        <p><%= new Date(post.createdAt).toDateString() %></p>
        <% if( currentUser && currentUser.id === "63a48c94b073ed35550a3f05" ) {
        %>
        <a class="btn" href="/delete/<%=post.id %>">Delete</a>
        <a class="btn" href="/update/<%=post.id %>">Update</a>
        <% } else{ %>
        <div></div>
        <% } %>
      </div>

      <% }) %> <%} else { %>
      <p>there are no posts to display 🙁</p>
      <% } %>
    </div>
  </body>
</html>
